[build]
  command = "mkdir -p public && cp index.html public/"
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["node-fetch"]
  
[functions.webhook]
  # Function-г public болгох (ямар ч эрхгүйгээр хандах боломжтой)
